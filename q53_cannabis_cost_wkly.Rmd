---
title: "q53 'On average, how much do you spend (in US dollars) on consumable cannabis products per week?'"
author: "David Ritter"
date: "2/18/2020"
output: html_document
runtime: shiny
---

```{r, message = FALSE}
library(tidyverse)
library(ggplot2)
library(stringr)
source("helper.R")
```

```{r}
raw_data <- read.csv("data/master.csv", stringsAsFactors = FALSE)
```

```{r}
q <- "On.average..how.much.do.you.spend..in.US.dollars..on.consumable.cannabis.products.per.week."
t <- "On average, how much do you spend (in US dollars) on consumable cannabis products per week?"
q_data <- get_single_selection_0(data = raw_data, q.name = q)
q_n <- length(unique(q_data$id))
```

```{r}
q_plot<- clean_single_select(q_data)
r <- plot_single_select(q_plot)
r
```

```{r}
write.csv(q_data, 'q53_cannabis_cost_wkly_144.csv', row.names = FALSE)
ggsave(filename = 'q53_cannabis_cost_wkly.png', plot = r, device = 'png')
```


```{r}
qf_data <- q_data %>%
  filter(response == "Response")
```

```{r}
qf_all_data <- 
  qf_data %>%
  inner_join(q_data, by = "id")

qf_n <- length(unique(qf_all_data$id))
```

```{r}
q_plot <- clean_single_select_f(qf_all_data)
r <- plot_single_select_f(q_plot)
r
```

```{r}
ggsave(filename = 'q53_cannabis_cost_wkly.png', plot = r, device = 'png')
write.csv(q_plot, 'q53_cannabis_cost_wkly_174.csv', row.names = FALSE)
```