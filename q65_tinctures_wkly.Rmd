---
title: "q65 'How many milliliters (mL) of tincture are you using per week? Note (30mL) = 1oz"
author: "David Ritter"
date: "2/17/2020"
output: html_document
runtime: shiny
---

```{r, message = FALSE}
# Load libraries
library(tidyverse)
library(ggplot2)
library(stringr)
source("helper.R")
```

```{r}
raw_data <- read.csv("data/master.csv", stringsAsFactors = FALSE)
```

```{r}
q <- 'How.many.milliliters..mL..of.tincture.are.you.using.per.week..Note..30mL...1oz'
q_final_response <- "X.311"

q_data <- get_choose_all(raw_data, q.name = q, f.response = q_final_response)
```

```{r}
table_1 <- get_single_selection_2(q_data, "Tinctures") %>%
  filter(response != "N/A") %>%
  clean_single_select_2()

table_1_n <- tally(table_1, count)

r1 <- plot_single_select(table_1) +
  labs(title = "Tinctures", subtitle = table_1_n)
r1
```


```{r}
# Save graph as PNG, plot data as csv, and all question data including response IDs as csv (save question n and N at end of plot file as "_x.x")
ggsave(filename = 'q65_Tinctures_wkly.png', plot = r1, device = 'png')
write.csv(table_1, 'q65_Tinctures_wkly_71.csv', row.names = FALSE)
```










```{r}
# Save graph as PNG, plot data as csv, and all question data including response IDs as csv (save question n and N at end of plot file as "_x.x")
ggsave(filename = 'q62_smoke_vape_wkly.png', plot = r, device = 'png')

write.csv(q_plot, 'q62_smoke_vape_wkly_144.csv', row.names = FALSE)

write.csv(q_data, 'q62_smoke_vape_wkly_ids.csv', row.names = FALSE)
```


```{r}
#OPTIONAL Filter one response, then move to process qf_data in the code of the other question being analyzed
qf_data <- filter(q_data, chosen == "Response")
```

```{r}
#OPTIONAL FILTERING!!!  Filter out responses from non-selected IDs. Run after step that generates q_data.
qf_all_data <- 
  qf_data %>%
  inner_join(q_data, by = "id")

q_n <- length(unique(qf_all_data$id))
```

```{r}
#OPTIONAL FILTERING!!!
q_plot<- clean_choose_all(qf_all_data)

r <- plot_choose_all(q_plot) +
  labs(title = "Which health conditions do you find cannabis to be helpful in treating? (Please select all that apply)", subtitle = q_n)
r
```

```{r}
# Save graph as PNG, plot data as csv, and all question data including response IDs as csv (save question n and N at end of plot file as "_x.x")
ggsave(filename = 'q#_qtext.png', plot = r, device = 'png')

write.csv(q_plot, 'q#_qtext_XX.csv', row.names = FALSE)

write.csv(qf_all_data, 'q#_qtext_ids.csv', row.names = FALSE)
```
