---
title: "Q61 Barriers (dav)"
author: "DJ"
date: "August 8, 2019"
output: html_document
runtime: shiny
---

```{r, message = FALSE}
# Load libraries
library(tidyverse)
library(ggplot2)
library(stringr)
source("helper.R")
```

```{r}
# Read in raw data from the dav survey. These are the files downloaded from survey monkey saved as a .csv
raw_data_dav <- read.csv("data/dav_nat.csv", stringsAsFactors = FALSE)
```

```{r}
# Pull out all responses for Q61 (a choose all question), combine, turn into a tidy data format for further processing then determine 'question n'
Q61 <- "Do.you.face.any.of.the.following.barriers.for.you.to.consume.cannabis...Please.select.all.that.apply."
Q61_final_response <- "X.279"

Q61_dav <- get_choose_all(raw_data_dav, q.name = Q61, f.response = Q61_final_response)

Q61_all <- 
  rbind(Q61_dav) %>%
  tidy_choose_all() %>%
  inner_join(ma_ids, by = "id")

number_of_people <- length(unique(Q61_all$id))
```

```{r}
# Manipulate data into a graphable format
Q61_plot <-
  Q61_all %>%
  group_by(chosen) %>%
  summarize(count = n()) %>%
  mutate(percent_all = count / sum(count)) %>%
  mutate(percent_responded = count / number_of_people)
```

```{r}
# Plot with percents and response counts
r <- 
  ggplot(Q61_plot, aes(x = reorder(chosen, count), y = percent_responded)) +
  geom_bar(stat = 'identity') +
  coord_flip() +
  geom_text(aes(label= paste(round(percent_responded,2), count, sep = "***")))

r
```

```{r}
# Save graph as PNG, plot data as csv, and all question data including response IDs as csv (save question n and N at end of plot file as "_x.x")
ggsave(filename = 'dav_q61_barriers.png', plot = r, device = 'png')

write.csv(Q61_plot, 'dav_q61_barriers_45.csv', row.names = FALSE)

write.csv(Q61_all, 'dav_q61_barrers_ids.csv', row.names = FALSE)
```
