---
title: "q68 'Does your VA doctor or clinician support your cannabis use?'"
author: "David Ritter"
date: "2/18/2020"
output: html_document
runtime: shiny
---

```{r, message = FALSE}
library(tidyverse)
library(ggplot2)
library(stringr)
source("helper.R")
```

```{r}
raw_data <- read.csv("data/master.csv", stringsAsFactors = FALSE)
```

```{r}
q <- "Does.your.VA.doctor.or.clinician.supportÂ.yourÂ.cannabis.use."
t <- "Does your VA doctor or clinician support your cannabis use?"
q_data <- get_single_selection(data = raw_data, q.name = q)
q_n <- length(unique(q_data$id))
```

```{r}
q_plot<- clean_single_select(q_data)
r <- plot_single_select(q_plot)
r
```

```{r}
ggsave(filename = 'q68_va_cannabis_use_support.png', plot = r, device = 'png')
write.csv(q_plot, 'q68_va_cannabis_use_support_130.csv', row.names = FALSE)
```

```{r}
qf_data <- q_data %>%
  filter(response == "Response text")
```

```{r}
qf_all_data <- 
  qf_data %>%
  inner_join(q_data, by = "id")

qf_n <- length(unique(qf_all_data$id))
```

```{r}
q_plot <- clean_single_select_y(qf_all_data)
r <- plot_single_select_y(q_plot)
r
```

```{r}
ggsave(filename = 'q68_va_cannabis_use_support.png', plot = r, device = 'png')
write.csv(q_plot, 'q68_va_cannabis_use_support_305.csv', row.names = FALSE)
```