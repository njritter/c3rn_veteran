---
title: "q45 'What prescription medications are you currently taking for the health conditions and symptoms you face that you are actively trying to reduce the use of by using cannabis? (Please select all that apply)'"
author: "David Ritter"
date: "2/18/2020"
output: html_document
runtime: shiny
---

```{r, message = FALSE}
library(tidyverse)
library(ggplot2)
library(stringr)
source("helper.R")
```

```{r}
raw_data <- read.csv("data/master.csv", stringsAsFactors = FALSE)
```

```{r}
q <- 'What.prescription.medications.are.you.currently.taking.for.the.health.conditions.and.symptoms.you.face.that.you.are.actively.trying.to.reduce.the.use.of.by.using.cannabis...Please.select.all.that.apply.'
q_final_response <- "X.249"
```

```{r}
filtered_data <- filter(raw_data, raw_data$X.247 != "I'm not trying to reduce any medication use" & raw_data$X.246 != "I donâ???Tt take any medications" )
```

```{r}
less_raw_data <- 
    filtered_data %>%
    select(
      q2,
      q:q_final_response)
```




```{r}
filtered_data <- filter(q_data, How.would.you.rate.your.quality.of.health.today. == "Fair")
```

















```{r}
q <- 'What.prescription.medications.are.you.currently.taking.for.the.health.conditions.and.symptoms.you.face.that.you.are.actively.trying.to.reduce.the.use.of.by.using.cannabis...Please.select.all.that.apply.'
q_final_response <- "X.249"
t <- "What prescription medications are you currently taking for the health conditions and symptoms you face that you are actively trying to reduce the use of by using cannabis? (Please select all that apply)"
q_data <- get_choose_all(raw_data, q.name = q, f.response = q_final_response) %>%
  tidy_choose_all()
q_n <- length(unique(q_data$id))
q_plot <- clean_choose_all(q_data)
r <- plot_choose_all(q_plot)
r
```

```{r}
ggsave(filename = 'output/q45_meds_reduced_cannabis.png', plot = r, device = 'png')
write.csv(q_plot, 'output/q45_meds_reduced_cannabis_144.csv', row.names = FALSE)
write.csv(q_data, 'output/q45_meds_reduced_cannabis_ids.csv', row.names = FALSE)
```

```{r}
qf_data <- q_data %>%
  filter(chosen == "Response text")
```

```{r}
qf_all_data <- 
  qf_data %>%
  inner_join(q_data, by = "id")

qf_n <- length(unique(qf_all_data$id))

qf_data <- qf_all_data[3] %>%
  group_by(chosen) %>%
  summarize(count=n()) %>%
  mutate(percent_responded = round(100*(count / qf_n), 2)) %>%
  arrange(desc(count))
```

```{r}
q_plot <- clean_choose_all_f2(qf_data)
r <- plot_choose_all_f2(q_plot)
r
```

```{r}
ggsave(filename = 'output/q45_meds_reduced_cannabis.png', plot = r, device = 'png')
write.csv(q_plot, 'output/q45_meds_reduced_cannabis_333.csv', row.names = FALSE)
write.csv(qf_all_data, 'output/q45_meds_reduced_cannabis_ids.csv', row.names = FALSE)
```

